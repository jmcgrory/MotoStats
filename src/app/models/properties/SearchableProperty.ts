import Property from './Property';

/**
 * @todo Decide whether this has any use
 */
abstract class SearchableProperty extends Property {

    constructor(newValue: any) {

        super(newValue);

    }

}

export default SearchableProperty;
